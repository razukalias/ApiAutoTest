<UserControl x:Class="TestAutomation.UI.Wpf.Views.Editors.BearerTokenAuthEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Token:" Margin="0,0,0,3"/>
        <TextBox Grid.Row="1" Text="{Binding Token}"/>
    </Grid>
</UserControl>