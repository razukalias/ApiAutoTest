<UserControl x:Class="TestAutomation.UI.Wpf.Views.Editors.HttpStepEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="Name:" Margin="0,5,0,0"/>
            <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Text="Method:" Margin="0,10,0,0"/>
            <ComboBox ItemsSource="{Binding HttpMethods}" SelectedItem="{Binding Method}"/>

            <TextBlock Text="URL:" Margin="0,5,0,0"/>
            <TextBox Text="{Binding Url, UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>

        <TabControl Grid.Row="1" Margin="0,15,0,0">
            <TabItem Header="Headers">
                <DataGrid ItemsSource="{Binding Headers}" AutoGenerateColumns="False" CanUserAddRows="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Key" Binding="{Binding Key}"/>
                        <DataGridTextColumn Header="Value" Binding="{Binding Value}"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
            <TabItem Header="Body">
                <xctk:WatermarkTextBox Watermark="Enter JSON body..." Text="{Binding BodyString}" AcceptsReturn="True" Height="200"/>
            </TabItem>
            <TabItem Header="Assertions">
                <DataGrid ItemsSource="{Binding Assertions}" AutoGenerateColumns="False" CanUserAddRows="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Path" Binding="{Binding Path}"/>
                        <DataGridComboBoxColumn Header="Operator" ItemsSource="{Binding Source={x:Static core:Enum.GetValues(typeof(core:Operator))}}" SelectedItemBinding="{Binding Operator}"/>
                        <DataGridTextColumn Header="Expected" Binding="{Binding ExpectedValue}"/>
                        <DataGridComboBoxColumn Header="Behavior" ItemsSource="{Binding Source={x:Static core:Enum.GetValues(typeof(core:AssertionBehavior))}}" SelectedItemBinding="{Binding Behavior}"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>