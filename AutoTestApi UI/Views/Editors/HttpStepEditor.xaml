<UserControl x:Class="TestAutomation.UI.Wpf.Views.Editors.HttpStepEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:helpers="clr-namespace:TestAutomation.UI.Wpf.Helpers">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Name -->
        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="Name" FontWeight="Bold" Margin="0,0,0,3"/>
            <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Padding="4"/>
        </StackPanel>

        <!-- Method & URL -->
        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <ComboBox Grid.Column="0" 
                      ItemsSource="{x:Static helpers:HttpMethodHelper.SupportedMethods}" 
                      SelectedItem="{Binding Method}" 
                      DisplayMemberPath="Method" 
                      Margin="0,0,8,0" 
                      Padding="4"/>
            <TextBox Grid.Column="1" 
                     Text="{Binding Url, UpdateSourceTrigger=PropertyChanged}" 
                     Padding="4"/>
        </Grid>

        <!-- Headers -->
        <GroupBox Header="Headers" Grid.Row="2" Margin="0,0,0,10" Padding="5">
            <DataGrid ItemsSource="{Binding Headers}" 
                      AutoGenerateColumns="False" 
                      CanUserAddRows="True" 
                      HeadersVisibility="Column"
                      MinHeight="100">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Key" Binding="{Binding Key}" Width="*"/>
                    <DataGridTextColumn Header="Value" Binding="{Binding Value}" Width="2*"/>
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>

        <!-- Body -->
        <GroupBox Header="Body" Grid.Row="3" Padding="5">
            <TextBox Text="{Binding BodyString, UpdateSourceTrigger=PropertyChanged}" 
                     AcceptsReturn="True" 
                     TextWrapping="Wrap" 
                     VerticalScrollBarVisibility="Auto"
                     FontFamily="Consolas"
                     MinHeight="150"/>
        </GroupBox>
    </Grid>
</UserControl>