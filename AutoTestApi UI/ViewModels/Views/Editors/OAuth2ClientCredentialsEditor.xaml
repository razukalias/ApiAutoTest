<UserControl x:Class="TestAutomation.UI.Wpf.Views.Editors.Auth.OAuth2ClientCredentialsEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Token Endpoint:"/>
        <TextBox Grid.Row="1" Text="{Binding TokenEndpoint}"/>
        <TextBlock Grid.Row="2" Text="Client ID:"/>
        <TextBox Grid.Row="3" Text="{Binding ClientId}"/>
        <TextBlock Grid.Row="4" Text="Client Secret:"/>
        <TextBox Grid.Row="5" Text="{Binding ClientSecret}"/>
        <TextBlock Grid.Row="6" Text="Scopes (space separated):"/>
        <TextBox Grid.Row="7" Text="{Binding Scopes}"/>
    </Grid>
</UserControl>